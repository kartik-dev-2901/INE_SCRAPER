function ProductSearch({ loading, query, results, trackingId, onSearch, onTrack }) {
    return <section className="search-panel"><div className="intro-copy"><p className="eyebrow">CATALOG SEARCH</p><h2>Know the right moment to buy.</h2><p>Search the catalog and start tracking the products that matter to you.</p></div><label className="search-field"><span className="search-icon">⌕</span><input type="text" placeholder="Search by product name..." value={query} onChange={onSearch} />{loading && <span className="search-loader">Searching</span>}</label>{results.length > 0 && <div className="search-results">{results.map(product => <article className="search-result" key={product.id}><div className="result-copy"><h3>{product.name}</h3><p>{product.brand} · {product.category}</p><small>SKU {product.sku}</small></div><button className="button button-primary" onClick={() => onTrack(product)} disabled={trackingId === product.id}>{trackingId === product.id ? "Adding..." : "Track price"}</button></article>)}</div>}</section>;
}
export default ProductSearch;
