import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
function PriceHistory({ chartData, history }) {
    return <section className="data-card"><div className="data-card-header"><p className="eyebrow">TREND</p><h3>Price history</h3><p>Successful scrape observations over time.</p></div>{chartData.length === 0 ? <div className="empty-data">No price history available.</div> : <div className="chart"><ResponsiveContainer width="100%" height={300}><LineChart data={chartData}><CartesianGrid stroke="#e9edf3" strokeDasharray="4 4" vertical={false} /><XAxis dataKey="time" tickLine={false} axisLine={false} /><YAxis tickLine={false} axisLine={false} /><Tooltip formatter={value => `₹${Number(value).toLocaleString()}`} /><Line type="monotone" dataKey="price" stroke="#5b63f6" strokeWidth={3} dot={{ fill: "#fff", stroke: "#5b63f6", strokeWidth: 2, r: 4 }} /></LineChart></ResponsiveContainer></div>}{history.length > 0 && <div className="table-wrap"><div className="table-row table-header"><span>Time</span><span>Price</span><span>Stock</span></div>{history.map(record => <div className="table-row" key={record.id}><span>{new Date(record.scraped_at).toLocaleString()}</span><strong>₹{record.price.toLocaleString()}</strong><span>{record.stock_status}</span></div>)}</div>}</section>;
}
export default PriceHistory;
